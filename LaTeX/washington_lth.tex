%! Author = Len Washington III
%! Date = 5/5/26

% Preamble
\documentclass[
	title={Lottery Ticket Hypothesis for Large Language Models}
]{cs595project}

\newboolean{showcaption}
\newcommand{\ROGUE}{ROGUE}
\newcommand{\ROUGE}{ROGUE}

\newcommand{\ablationunix}{1778157329} % 1778198132
\providecommand{\ablationunix}{1778157329}

\RequirePackage{svg}

% Document
\begin{document} % Should be 6-8 pages.
%
\begin{center}
	{\bfseries\Huge Using the Lottery Ticket Hypothesis to Decrease the Size of LLMs}\\
	{\bfseries \Large Author: \href{mailto:lwashington3@hawk.illinoistech.edu}{Len Washington III}\orcid{0009-0004-0165-6316} (A20484223)}\\
	{\bfseries \Large Artifacts: \href{https://github.com/lwashington3/cs595-2-project}{\faIcon{github} lwashington3/cs595-2-project}\\ \href{https://huggingface.co/collections/lwashington3/lottery-ticket-hypothesis-for-llm}{\includesvg[width=1em]{hugging-face-brands-solid-full.svg} lwashington3/lottery-ticket-hypothesis-for-llm}}\\
\end{center}

% Indicate whether your project is mainly: % TODO: Which am I?
%- Research prototype
%- Systemization/reproduction <-
%- Engineering/measurement

\section{Motivation}\label{sec:motivation} % and problem statement
% Clearly explain the efficiency problem you study. Why does it matter for ML systems, efficient deep learning, or AI accelerators?
Transformers and Large Language Models have allowed for more efficient execution of NLP tasks.
One such example where LLMs have been implemented is the \href{https://exsclaim.materialeyes.org}{EXSCLAIM} pipeline,\supercite{EXSCLAIM} which utilizes LLMs to split captions from research papers to match with any subfigures within the overall (master) figure.
However, the biggest problem with running EXSCLAIM on its current servers is that there are no GPUs, which significantly reduces how fast the pipeline can run.
Coupled with the dozens of captions that may need to be separated, the Caption Distributor is easily the largest bottleneck within the pipeline.
The default LLM (Llama 3.2) can do a fair job of splitting these captions, though it will either need to be replaced by a better model (which would have to be larger) or have some type of improvement done.
To address these issues, a method was sought out to increase the inference speed without disrupting the current accuracy of the models.
A pruning technique known as the Lottery Ticket Hypothesis was chosen, since it had claims of reducing a neural network's weights by 95\% while still maintaining similar accuracies as the unpruned version.

\section{Background}\label{sec:background}
% Summarize the most relevant prior work. Cite papers, codebases, datasets, tools, and any external resources you used.
The Lottery Ticket Hypothesis states that ``a randomly-initialized, dense neural network contains a subnetwork that is initialized such that--when trained in isolation--it can match the test accuracy of the original network after training for at most the same number of iterations.''\supercite{Lottery-Ticket-Hypothesis}
Essentially, by training a randomly-initialized neural network, a subnetwork, or winning ticket, can be discovered and retrained from the beginning, matching the accuracy of the original model.

The paper describes two methods towards pruning a network, one-shot pruning, and iterative pruning.
For one-shot pruning, the trained network is pruned once, with $p\%$ of weights being masked out (pruned) while the reset are reset to the initially-random values.
There is also iterative pruning, which repeatedly trains, prunes, and resets the network over $n$ rounds, each time pruning another $p^\frac{1}{n}$\%.
\citeauthor{LTH-Transformer} describe further methods for utilizing the Lottery Ticket Hypothesis with transformers.
One such method recommends applying the computed mask $m$ to one of the earlier checkpoints from when initially-randomized model is trained, rather than the initial model.
%This ``iterative magnitude pruning with rewinding''
However, the paper does not go into more detailed implementation of such methods.

Since EXSCLAIM's pipeline was summarizing scientific captions into a list of strings, a similar dataset was sought with hopes of further use for EXSCLAIM.
As such, one dataset stood out, \href{https://huggingface.co/datasets/MMInstruction/ArxivCap}{MMInstruction/ArxivCap},\supercite{li-etal-2024-multimodal-arxiv} which was designed training parser to understand Arxiv.
The 755 GB dataset was filtered through, retaining only what was necessary, before re-uploading it to HuggingFace.
This dataset, \href{https://huggingface.co/datasets/lwashington3/ArxivCap-Minimal}{lwashington3/ArxivCap-Minimal}, however, did not seem to work well with most LLMs, and was replaced for the duration of the project with \href{https://huggingface.co/datasets/abisee/cnn_dailymail/tree/96df5e686bee6baa90b8bee7c28b81fa3fa6223d}{abisee/cnn\_dailymail}.

\section{Methodology}\label{sec:methodology}
% Describe your approach in enough detail that another student could understand and reproduce the main idea.
% Include system design, algorithms, implementation choices, datasets, workloads, models, hardware, and software environment when relevant.
To utilize the Lottery Ticket Hypothesis with an LLM that I did not initially train, nor possess the training dataset, several steps are required.
The first is to obtain the raw weights of a given LLM, as well as a dataset that would be used to fine-tune the model.
%Safetensors is the framework used to handle and manipulate raw tensor weights throughout this project, %given that it is a part of HuggingFace's suite
The model will also be preprocessed utilizing this dataset, to help bridge any assumptions made by~\cite{Lottery-Ticket-Hypothesis} having the initial training dataset for the pruning process.
Once the model has been preprocessed, each layer within the model will be subjected to magnitude pruning, where the weights are sorted by magnitude, then the smallest $p\%$ of parameters are masked, before training on the dataset.

To evaluate the results, a pipeline was constructed to run through the original (un-pruned) model, as well as several pruned models using the held-out portion of the dataset.
%Each pruned model was recomputed by applying the mask to its base model.%
%\footnote{The training pipeline could have saved the masked model directly, but it was more efficient to have a single base model saved, and apply several mask files (with a smaller datatype and storage requirement) than to save multiple larger models.}
Once the models have been loaded into memory, the system runs evaluate each model against the given row.
The model is called once using HuggingFace's native implementation (with additional timers) to measure how long it takes for the first token to be generated, as well as the overall run time.
After that, the model is called again, this time using PyTorch's implementation, to receive the logits from the output which can be used to calculate the Perplexity of the prediction.
The last metric, the \ROGUE-L F1 score, can be calculated with either the true value from the dataset or the predicted value of the baseline as the target.
For the following results, the \ROUGE-L was calculated using the predicted value of the baseline as the target value, since the purpose of this is to see how close the results of a pruned model can get to the original model.

\section{Experimental Setup}\label{sec:experimental-setup}
% Clearly state:
%- Hardware used, such as CPU, GPU, memory, cluster/cloud platform
%- Software stack, such as Python, PyTorch, CUDA, Triton, vLLM, etc.
%- Datasets, models, benchmarks, or workloads
%- Baselines and comparison methods
%- Metrics, such as latency, throughput, memory usage, accuracy, energy, cost, or scalability
\begin{table}[H]
	\centering
	\caption{Experimental Specifications}
	\label{tab:experiment-specs}
	\begin{tabular}{|c|c|}
		\toprule
		GPU & NVIDIA A100 80GB PCIe\\
		\midrule
		CPU & AMD EPYC 7763 64-Core Processor\\
		\midrule
		Cores & 256\\
		\midrule
		RAM & 503.414455 GB\\
		\midrule
		Disk Space & 418 GB\\
		\midrule
		Chameleon Cloud Site & CHI@UC\\
		\midrule
		Chameleon Cloud Node Name & gigaio-compute-04\\
		\bottomrule
	\end{tabular}
\end{table}

Python 3.14.4 was the only language involved, and the software stack was mostly composed of the HuggingFace suite, namely Transformers, Datasets, and Safetensors (the library used to access the raw tensor parameters).
The full list of libraries and versions can be seen in Appendix Section~\ref{sec:python-libraries}
The baseline model that was used for pruning and evaluation was \href{https://huggingface.co/google/gemma-3-1b-it/tree/dcc83ea841ab6100d6b47a070329e1ba4cf78752}{google/gemma-3-1b-it} (Gemma 3) along with \href{https://huggingface.co/datasets/abisee/cnn_dailymail/tree/96df5e686bee6baa90b8bee7c28b81fa3fa6223d}{abisee/cnn\_dailymail} (CNN DailyMail) as the dataset.
The metrics used to evaluate the effectiveness of pruning were Time-to-First-Token (TTFT), Time per Output Tokens (TPOT), ROGUE-L F1 score, and Perplexity.

\section{Results}\label{sec:results}
% Present your main results using tables or figures. Do not only report numbers; explain what the numbers mean. Discuss where your method works, where it does not, and why.
First, it should be noted that the original Gemma 3 model was not accurate when it came to summarizing the CNN DailyMail dataset.
These low results led me to substitute in Gemma's responses for any metrics that required the true value (namely \ROUGE-L) so the pruned models could be judged more accurately.

The results for the experiment turned out rather well, with the pruned models taking less time to run, while being able to produce the exact same response as the original model.%
\footnote{Note: this happens when the model has max\_new\_tokens set to 0. Anything more will result in extra tokens that do not make sense towards the output. Since the pipeline could not have max\_new\_tokens to anything less than 1, the last token for each output was ignored.}
The results for the first row of CNN DailyMail's test set that were and were not trained can be found in Tables~\ref{tab:evaluation-pipeline-1-trained} and~\ref{tab:evaluation-pipeline-1-untrained}, as well as Figures~\ref{fig:run-time-baseline}-\ref{fig:rogue-l-baseline}.
The only metric that was not better than the original was the perplexity, which shows that the pruned models are a lot less certain about their responses than the original model is.
However, given that the other metrics were improved, this does not matter as much.

The results for testing if pre-training the model beforehand can be seen in Figures~\ref{fig:run-time}-\ref{fig:tpot}.

\section{Profiling Analysis}\label{sec:profiling-analysis}
% Include evidence of careful measurement. This can include runtime breakdowns, memory profiling, GPU utilization, kernel-level profiling, communication overhead, or other system-level analysis.
\begin{itemize}
	\item Reading in models for the evaluation pipeline takes $\sim$40 seconds, which can lead to massive wait times if 77 models are being evaluated (leading to a wait time of about 51 minutes before the evaluation actually starts)
\end{itemize}

\section{Reproducibility}\label{sec:reproducibility}
% Explain how to reproduce your results. Include instructions for running the code, expected outputs, and any important dependencies.
The artifact can be found at \url{https://github.com/lwashington3/cs595-2-project}.
By cloning the repository, Docker Compose can be used to the LTH module, along with containing scripts used to automatically generate the figures and tables in this paper.
Running \code{docker compose run -it project} in the repo's root directory will run the ablation script with default values to generate masks for pruned models (pre-trained and not pre-trained), as well as the figures and tables during the evaluation.
Other scripts are available in the \href{https://github.com/lwashington3/cs595-2-project/tree/main/scripts}{scripts} folder, along with documentation for how to set your own variables.
With default values, the models should appear in the models folder, which will then be sorted by process name (default is ``Ablation \{UNIX timestamp that the script started\}''), training status (trained or untrained), then the pruning percentage used.
Within the \code{evaluation} folder, a folder with the same process name as before will contain a folder for each row in the test dataset.
Each of those folders will contain the figures (saved as PNG, SVG, and \LaTeX\ compatible-PGF formats), as well as the tables pre-processed in a \LaTeX\ file.
There will be two copies of each file's stem, one with \code{\_baseline}, which has the baseline value added, such as Figures~\ref{fig:run-time-baseline}-\ref{fig:rogue-l-baseline}, and one only containing the pruned models, such as Figures~\ref{fig:run-time}-\ref{fig:rogue-l}.

\section{Limitations}\label{sec:limitations}
% Discuss what remains incomplete, what assumptions your project makes
Currently, the pre-training process is limited by the size of the GPU.
Without a GPU, the training could run for hours without visibly finishing the first example.
Even then, a 32 GB Nvidia GeForce RTX 5090 pre-training Gemma could not fit any training rows onn the GPU, and the 80 GB GPU mentioned in Table~\ref{tab:experiment-specs} could only hold 6 rows.

\section{Future Works}\label{sec:future-works}
% What you would do next with more time.
\begin{itemize}
	\item Figure out how to have more control over the pre-training process so I can better control when training dataset rows are onloaded or offloaded from the GPU so more than 6 can work.
	\item Structurally remove transformer layers from the model and test how that affects it
	\item Figure out a better solution towards saving values (I could save the masked pre-trained value in a single file instead of having it distributed [this is only done because I don't want the same version of different files constantly written in different places]), but I not all values need to be written (save space)
\end{itemize}

\printbibliography[title={References}, heading=subbibnumbered]

\appendix
\section*{\textsc{Appendix}}
\renewcommand{\thesection}{\Alph{section}.}

\section{Python Libraries}\label{sec:python-libraries}
\inputminted{text}{../requirements.txt}

\section{Results Tables}\label{sec:results-tables} % TODO: Pruning parameter was set up as how much remains, not what was removed
Tables~\ref{tab:evaluation-pipeline-1-trained} and~\ref{tab:evaluation-pipeline-1-untrained} show the results for the same row of the dataset, grouped by pre-training status.
The first row for both details the baseline model that was not pruned.

% TODO: These need last column lines added and double last line removed
\input{../evaluation/ablation_\ablationunix/1/trained_results}

\input{../evaluation/ablation_\ablationunix/1/untrained_results}

\section{Results Figures}\label{sec:results-figures}
\begin{figure}[H]
	\centering
	\resizebox{\textwidth}{!}{\input{../evaluation/ablation_\ablationunix/1/run_time_baseline.pgf}}
	\caption{The total run time for each model.}
	\label{fig:run-time-baseline}
\end{figure}

\begin{figure}[H]
	\centering
	\resizebox{\textwidth}{!}{\input{../evaluation/ablation_\ablationunix/1/ttft_baseline.pgf}}
	\caption{The amount of time it took the model to generate its first response token (Time to First Token).}
	\label{fig:ttft-baseline}
\end{figure}

\begin{figure}[H]
	\centering
	\resizebox{\textwidth}{!}{\input{../evaluation/ablation_\ablationunix/1/output_token_time_baseline.pgf}}
	\caption{The average amount of time it took to generate each token in the response.}
	\label{fig:tpot-baseline}
\end{figure}

\begin{figure}[H]
	\centering
	\resizebox{\textwidth}{!}{\input{../evaluation/ablation_\ablationunix/1/rogue_l_baseline.pgf}}
	\caption{The \ROUGE\ F1 Score for the Longest Common Subsequence.}
	\label{fig:rogue-l-baseline}
\end{figure}

\section{Ablation Figures}\label{sec:ablations-figures}
\begin{figure}[H]
	\centering
	\resizebox{\textwidth}{!}{\input{../evaluation/ablation_\ablationunix/1/run_time.pgf}}
	\caption{The total run time for each model (without the baseline).}
	\label{fig:run-time}
\end{figure}

\begin{figure}[H]
	\centering
	\resizebox{\textwidth}{!}{\input{../evaluation/ablation_\ablationunix/1/ttft.pgf}}
	\caption{The amount of time it took the model to generate its first response token (Time to First Token) (without the baseline).}
	\label{fig:ttft}
\end{figure}

\begin{figure}[H]
	\centering
	\resizebox{\textwidth}{!}{\input{../evaluation/ablation_\ablationunix/1/output_token_time.pgf}}
	\caption{The average amount of time it took to generate each token in the response (without the baseline).}
	\label{fig:tpot}
\end{figure}

\begin{figure}[H]
	\centering
	\resizebox{\textwidth}{!}{\input{../evaluation/ablation_\ablationunix/1/rogue_l.pgf}}
	\caption{The \ROUGE\ F1 Score for the Longest Common Subsequence (without the baseline).}
	\label{fig:rogue-l}
\end{figure}

\end{document}
